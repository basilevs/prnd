-@ val it: Publication
%a(href={request.getContextPath+"/publications/"+it.id})
	= it.title
,
= it.year.toString
,
= collection(it.publishers.associationMap.map { case (p, ass) => ass }, separator = ",")


